// ── Wire Chrome — component barrel ───────────────────────────────────────────
// Import this single file to register all three wire chrome custom elements:
//   <wire-status-bar>  — accent line + status bar
//   <wire-header>      — app header bar (back, title, subtitle)
//   <wire-home-bar>    — bottom home indicator

import "./wire-status-bar.js";
import "./wire-header.js";
import "./wire-home-bar.js";
